#include "principal.h"

#include <QApplication>

int main(int argc, char *argv[])
{
    // Geovanny listo!
    // Hanner Participando
    // Justin Participando
    QApplication a(argc, argv);
    Principal w;
    w.show();
    return a.exec();
}
